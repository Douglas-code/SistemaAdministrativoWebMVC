@model IEnumerable<SistemaAdministrativoWebMvc.Models.Vendedor>

@{
    ViewData["Title"] = "Vendedores";
}

<h2>@ViewData["Title"]</h2>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataNascimento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SalarioBase)
            </th>
            <th>
                <a class="btn btn-primary btn-sm" asp-action="CadVendedor">Adicionar</a>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataNascimento)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SalarioBase)
                </td>
                <td>
                    <button class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@item.Id"> <span class="fa fa-edit"></span></button>
                    <button class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id"> <span class="fa fa-trash"></span></button>
                </td>
            </tr>
        }
    </tbody>
    
</table>

